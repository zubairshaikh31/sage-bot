# Required for production (Vercel deployment)
# Set this to your deployed Render backend URL
VITE_API_URL=https://your-backend.onrender.com/api

# Leave unset for local dev — the Vite proxy handles /api → localhost:3001
